quarkus:
  analytics:
    disabled: true

  http:
    port: 8081

  devservices:
    enabled: false

  datasource:
    username: clients
    password: clients
    jdbc:
      url: jdbc:postgresql://localhost:5433/clients

  hibernate-orm:
    log:
      sql: false
